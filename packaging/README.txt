GSPTaskMiningAgent portable package

Run START_AGENT.ps1 from PowerShell or start GSPTaskMiningAgent.exe directly.
The agent creates config.json and data folders next to the executable on first run.
Use ENABLE_AUTOSTART.ps1 and DISABLE_AUTOSTART.ps1 to manage current-user autostart.
